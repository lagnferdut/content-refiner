
import { Language, Purpose, Tone, DesiredLength } from './types';

export const LANGUAGE_OPTIONS = Object.values(Language);
export const PURPOSE_OPTIONS = Object.values(Purpose);
export const TONE_OPTIONS = Object.values(Tone);
export const LENGTH_OPTIONS = Object.values(DesiredLength);

export const APP_TITLE = "Kreator Treści";

export const GEMINI_MODEL_TEXT = "gemini-2.5-flash-preview-04-17";